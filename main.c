#include <mini 
#include "so_long.h"
int mait(int argc, char **argv)
{
  int i;

  if(argc<2){
   i=0; 
    while(*argv[i])
    {
      while(*arg[i]!='.')
        i++;
      if(*argv[i]!='d'&& *argv[++i]!='e' && *argv[++i]!='r')
        return (printf("Error file"),EXIT_FAILURE);
      i++;
    }

  } 
}
